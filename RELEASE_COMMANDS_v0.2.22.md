# Release commands for v0.2.22

```bash
git status
git add -A
git commit -m "Replace disabled pump button with text state"
git push origin main

git tag -a v0.2.22 -F RELEASE_NOTES_v0.2.22.md
git push origin v0.2.22
```
