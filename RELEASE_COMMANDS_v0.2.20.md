# Release commands for v0.2.20

```bash
git status
git add -A
git commit -m "Improve disabled pump controls"
git push origin main

git tag -a v0.2.20 -F RELEASE_NOTES_v0.2.20.md
git push origin v0.2.20
```
