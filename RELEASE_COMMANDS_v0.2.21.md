# Release commands for v0.2.21

```bash
git status
git add -A
git commit -m "Fix sensor UI and pump control performance"
git push origin main

git tag -a v0.2.21 -F RELEASE_NOTES_v0.2.21.md
git push origin v0.2.21
```
